# Git

```shell
git init
git add .
git commit -m "messgae"
git push
git pull
git clone "url"
git status
git log
git branch branch_name
git checkout branch_name
git merge branch_name
git remote -v
```
